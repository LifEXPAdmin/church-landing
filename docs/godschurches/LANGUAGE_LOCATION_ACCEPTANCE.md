# Language and private discovery settings acceptance

## Verified publication — September 16, 2026 UTC

**2026.09.16.5 / cd74d94c6c00f66a95fc3c0c08a23905d2ba52ff** is READY in
**dpl_BzGk6gncWPzxJWsBH6C1rwSg6BGk**. READY was recorded at 06:10:47 UTC;
the independent canonical alias and actual serving SHA/version matched at
06:11:09 UTC. Runtime source is `797d386`; the subsequent commit changes only
the browser test's accepted-response wait.

The new Language and location folder uses existing account-bound discovery,
profile and calendar controls. English interface information, reading-language
filtering, manual town/radius and shared profile location are explained separately.
Settings search and Help expose these destinations. The interface-language detail
loads separately; no added dependency, duplicate preference store, backend,
permission, schema, provider or geolocation request is introduced.

Remaining regional-format, further interface-language, profile-location audience
and device-location/translation capabilities are not represented as working
controls. Existing private account/version/retry and profile-disclosure boundaries
remain authoritative. Verification uses fictional isolated accounts; actual live
checks were read-only.

## Verification and preservation

Nineteen focused checks pass: registry/safe returns, Help, release content,
discovery validation/catalog provenance, actual HTTPS account-pinned discovery
and Settings-context isolation. Types, scoped lint and website-copy guard pass
(707 files, 46,130 authored fragments). The clean production build passes
189 runtime traces, 43,672 entries and 476 server JavaScript files. The actual
provider build passes 189 traces, 59,760 entries and 475 server JavaScript files.
Neither graph contains private fixtures/environment files or Prisma config tools.

Four production-browser groups pass with no JavaScript errors:

- Search, folder and keyboard navigation; English-only guidance; 320/390/1440px
  and doubled text, with phone rendering inspected.
- Device location denied, manual town/radius and reading-language save/reload,
  and unchanged optional shared profile location.
- Accepted save with a lost response, focus revalidation and exact original
  request recovery without another version increment.
- Unavailable Settings reads conceal controls; signed-out entry preserves only
  the registered return destination.

The first local build rejected a root fixture symlink. Replacing it with the
established excluded child-directory layout fixed the fixture setup without
relaxing the guard. The first HTTPS test invocation used an older HTTP fixture
origin; the guarded current HTTPS environment passes. The new browser assertion
initially read the database while the accepted response was still in flight;
waiting for the actual enabled retry state fixes the test. Initial failure logs
are retained separately from final passing evidence.

Live checks pass 19 public/navigation/access groups, four current health groups
and three actual signed-in Chrome observations. The new folder, full guidance
and canonical discovery editor render the exact current release; the existing
Latest choice and two unread notifications remain unchanged. Public town lookup
returns bounded catalog labels, and private preferences remain denied to guests.
The canonical hydration renderer is 173,096 bytes, SHA-256
`2b7c5f99a8710e52520e7d0dc25c9fb65fd7c06e0a1d6cfee97276e0a452a3b7`.

All 89 production migration checksums match with no pending migrations. All
121 table fingerprints are unchanged from 06:02:28 through 06:13:47 UTC. The
protected restore receipt remains current at 03:13:36 UTC. Current health has no
alerts or pending work. Error/fatal queries from READY through 06:13 UTC return
no rows (limit 100 per level). Verification application writes, actual sends and
device-location requests are zero. Physical-device acceptance is not inferred.

## Measured runtime cost

The unique Settings route plus root/platform layout JavaScript increases from
188,353 to 188,688 per-file gzip bytes: **335 bytes**. Help increases from
242,176 to 242,708: **532 bytes**. The separately loaded language detail adds
1,086 gzip bytes (2,877 raw). These local bundle sums are not network-transfer or
latency measurements. No extra private read or mutation service is introduced.

The provider queued this deployment for 164.416 seconds behind the already
documented second project that builds the same main-branch source. Its purpose
and configuration remain unchanged. This is build contention, not request
latency or a reason to change production compute.
