# Optional device-area suggestions acceptance

September 16, 2026 UTC. Candidate for `2026.09.16.7`, not a live completion
receipt. Regional/private-location prerequisites must complete their full gate
and exact release before this follow-through is published.

Runtime checkpoint `e8644ee` implements the
[device-location contract](DEVICE_LOCATION_CONTRACT.md): explicit verified-adult
preflight, one low-accuracy browser request, immediate quarter-degree conversion,
transient selected-country catalog lookup, at most five public area labels, and
separate selection and Save through the existing discovery owner. No coordinate
store, schema, provider, dependency, tracking watcher or automatic request is
added. The optional translation row stays unavailable. Help, public feature
content, release notes and the privacy notice explain the actual behavior.

Thirty-one focused service/contract checks pass, covering coarse bounds and
staleness, selected-country lookup, malformed and precise inputs, active/adult
account pinning, origin and private headers, unchanged preferences/profile and
command/recovery records, existing discovery feeds, Settings and release content.
Types, scoped lint, copy checks and the production build pass. The final build
contains 191 checked runtime traces, 44,226 trace entries and 481 server
JavaScript files, with no private fixture/environment/configuration-loader leak.
The new route explicitly includes the existing catalog shards.

All 25 production-build browser groups pass on `e8644ee`: seven device/accessibility,
four language/location, eleven discovery and three post-classification groups.
They cover a real browser Geolocation API with an emulated position and grant;
mocked denial, failure, timeout and inaccurate results; a permission-prompt wait
bounded independently of the native timeout; cancelled and obsolete callbacks;
manual save and exact lost-response retries; inactive, switched and guest access;
precise-input denial over HTTPS; and unchanged profile disclosure. Browser
errors, production writes and external sends are zero. This does not verify a
physical device, native OS prompt or real GPS position.

The first device browser run exposed a fixture-only BigInt serialization error
after the native grant and explicit-save assertions had passed. The fixture now
serializes its database snapshot safely. All failed and successful logs remain
in private evidence. Final doubled-text phone/desktop layouts were checked.
A focused 300% text check reproduced an unfocused skip link overlapping content
by 125.59375 pixels. Its hiding offset now follows its own height; the same check
ends at the viewport edge, and keyboard focus/activation passes at 200% and 300%.

The lazy device module is absent from the ordinary route's static asset set.
Against the regional candidate, unique emitted JavaScript gzip totals change by
186 bytes on Home and Feed, minus 405 bytes on Settings and 42 bytes on the
profile editor. These are gzip-level-9 build asset comparisons, not latency or
network-transfer measurements. The helper adds no request before explicit use.
Twenty-nine isolated public/access checks pass for exact candidate identity,
privacy/help/features/release copy and widths, guest returns, minimal catalog
responses and private endpoint denials. They are not canonical-live evidence.

Remaining: prerequisite full-gate and release acceptance, integration into the
release branch, exact READY/canonical/serving identity, current signed-in control
inspection without requesting the owner's position, live public/health checks,
production data comparison, and Notion/Todoist readback. Actual device and
translation-provider acceptance retain their distinct scopes.
