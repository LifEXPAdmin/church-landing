# Event privacy navigation and church calendar scope

September 22, 2026. Verified live as `2026.09.22.5`. Main application
`94ea771a811dcdd9fc8c909bd6b2b16f70925fb6` is READY in
`dpl_7qD3C9dsPxwfCuSMwWwUyU56GxDw`, independently canonical at
`godschurches.com` with matching serving identity.

Authorized agenda rows and event pages link directly to the existing personal
sharing or church publication controls. Church calendar listing entries link to
administration with the selected church visible. Calendar and event screens
reuse their current source label and edit/publication booleans to show current
duties and the canonical roles/permissions destination. Calendar Settings links
to that existing listing and keeps church administration separate from personal
preferences. No query, grant, permission, schema or store is added. The current
service still checks scope, eligibility, revocation and confirmation on writes.

Current local evidence: thirty-two service/HTTP checks and twelve built-browser
groups pass, including calendar/navigation/Settings/release and seven actual
HTTP checks plus the existing Calendar Settings and sharing-preview regressions. The four new built-browser groups pass: exact privacy destinations;
personal view/default changes retaining private event state; church editor
versus publisher duties with forged publication denied; revocation of an already
open publisher form; second-church isolation and member/ineligible/guest denial.
The 320/390/1280-pixel enlarged-text checks pass with no browser errors. Two
initial fixture failures were corrected by leaving the prior document before
changing fictional login cookies, including the guest fragment-link case.
Application runtime was unchanged during these harness corrections.

TypeScript, focused lint, copy and the clean production build pass. Six runtime
files match the built candidate. Canonical services, permission checks, schema,
recovery and dependencies are unchanged. The build checks 231 runtime traces,
76,843 entries and 573 server JavaScript files. Existing hydration output remains
173,096 bytes with the accepted checksum. Measured route client chunks retain
12/13/14 chunks for calendar/event/Settings respectively; compressed totals grew
by 136/149/126 bytes. No extra query or request is introduced and no speed
improvement is claimed. Previous application rollback needs no data conversion.

The fresh encrypted backup and protected restore at 06:23:43 UTC preserved all
147 table/column fingerprints and verified all 105 migrations. Temporary plain
restoration data was removed. No production mutation or provider send occurred.
Final release acceptance at 06:39 UTC passed eleven anonymous route/API/browser
checks and six protected health checks. All 147 production fingerprints remain
unchanged, all 105 source/production/recovery migration checksums match, and no
new or pending migration exists. The hosted build passed 231 traces, 76,750
entries and 572 server JavaScript files. Browser errors and successful scoped
runtime error/fatal queries returned zero rows during 06:37:53.131 to
06:39:12.297 UTC. Verification writes, grants and sends were zero. The exact
private task was completed and its Session Log reconciled. Authenticated editing
used fictional built fixtures; no physical-device or production-member editing
acceptance is claimed. Saved following, display defaults and subscriptions
remain separately owned work.
