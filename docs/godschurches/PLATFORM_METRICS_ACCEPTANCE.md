# Platform growth measurement core

## Shared feedback integration — September 16, 2026 UTC

The actual feedback ratings, displayed-exposure coverage, source categories and
weekly context now ship in **2026.09.16.1 / 799549d**, exact READY/canonical
deployment **dpl_9wyYfbSuGveYmYHtG4DzDPYdPhDG**. Growth and its separately permitted
CSV share the same current-source math, suppression, withdrawal and coverage rules.
The staged 163-file gate, 35 browser groups and live/recovery checks pass; see
[complete feedback acceptance](FEEDBACK_WEEKLY_ACCEPTANCE.md). The former source
integration dependency is cleared. Actual privileged production report access,
operator setup and physical/pilot acceptance remain unverified. No measurement
consent, QA access grant or production fixture was created.

## Original core release receipt

September 15, 2026 UTC. Product **2026.09.15.7**, application commit
**944d382d12742deec0aa36071dc30ffc444399d8**, is **READY** in
**dpl_4ppv8oT9ZRKMBA4xeDiicHhQ1TcF** as of **19:50:59 UTC**. The independent
canonical alias and actual release endpoint match that deployment and SHA.
Core engineering is released. Feedback-owned source integration and actual
privileged production report acceptance remain open; no QA access grant was made.
Later receipt-only commits do not change this serving identity.

## Released behavior

The [metric contract](PLATFORM_METRICS_CONTRACT.md) defines current-source growth,
signup methods, lifecycle balance, consent coverage, ordinary adoption, exact-day
return cohorts and case workload. Private Growth reports add date presets, custom
windows, chart/table equivalents, honest incomplete/suppressed/empty states and
separately authorized aggregate CSV exports with audit hashes. Current access is
checked again for reports and every export; a saved response cannot survive
revocation or an account switch.

Optional measurement is off by default. Its separate settings control supports
withdrawal, optional referral/device choices, retained conflicting edits and
exact retries. Narrow trusted foreground navigation and observed onboarding
signals exclude reading time, page addresses, messages, prayer content and faith
scores. Raw facts are limited to 90 days and filtered before maintenance. New
signup methods are recorded once; linking Google does not rewrite the original
method. Historical methods, actions and choices are not inferred. Existing
operational accounts are excluded from optional member measurement.

Withdrawal, operator assignment, erasure, expiry and protected restoration clear
or exclude optional facts. Restored data receives a new lifecycle baseline;
restoration cannot opt people back in. Current source audiences, parent visibility,
church connections, topic availability and calendar access remain authoritative.
The application adds no runtime dependency or new primary navigation destination.

## Isolated verification

Fictional fixtures use loopback PostgreSQL 17 and verified local HTTPS. Browser
acceptance runs the actual production build with transport disabled; test-only
delivery sinks never send to real recipients.

- The staged established gate covers all **155 discovered files**. Successful
  upgrade, full restore, fresh schema, development checks, production builds,
  HTTPS restart and service stages are retained. All 88 remaining files pass
  after correcting the schema-only recovery fixture's missing baseline. A further
  focused source test passes separately, bringing current discovered coverage to
  156 files. Failed attempts remain preserved rather than relabeled as passes.
- Six report/export/onboarding/support scenarios verify the frozen A2
  10/8/6/4/2 cohort and 80/60/40/20 percentages; mature-day boundaries; original
  timestamps; database-session timezone invariance; separate export authority;
  actual owner exports; savepoint isolation; unique cases and substantive human
  responses; and existing church activation without duplicate listings.
- Five collection/lifecycle scenarios cover original signup methods, versioned
  cursors, concurrency, optional dimensions, operator-assignment withdrawal,
  account suspension, expiry and restored-choice clearing. Three actual
  maintenance/protected restore checks pass. The additional adoption scenario
  exercises follows, posts, replies, RSVPs, volunteers and events, then rechecks
  withdrawal, lost church access, hidden reply ancestors and prayer/repost exclusion.
- Eight final browser groups plus runtime/cost checks pass without browser errors:
  320-pixel and keyboard reports; actual CSV and matching audit hash; all report
  states; reconnect/revocation; default-off choices; trusted foreground input;
  cross-device conflicts; interrupted exact retry; withdrawal; and identity/access
  concealment. Twenty-seven additional public/private-denial preview probes pass.
- All 986 application/schema/data/configuration files match the final tested
  preview. Later differences are documentation and test fixtures. Final types,
  focused lint, production build, hydration verification and runtime traces pass.

Verification repaired a UTC binding defect, stale preset date inputs, narrow table
word wrapping, and complete off-state normalization after operational assignment.
Historical/new-schema comparisons and schema-only recovery fixtures now assert
the required baseline explicitly. Initial fixture, selector, retry-race and
event-version failures remain in private logs with their passing corrections.

## Production, recovery and live acceptance

At **19:04:12 UTC**, an encrypted, verified-TLS production copy upgraded from 63 to
69 migrations in an isolated cluster. Every original column across **107 tables**
matched after the upgrade and protected replay completed. Plaintext restore data
was removed. The provider then applied the six additive migrations. At
**19:50:42 UTC**, all **69 production checksums matched**, with none pending.

The installed recovery registry preserves all 63 prior checksums and includes all
69 current migrations without changing the installed retention implementation.
Its new ordinary **69-to-69** encrypted backup restore passed at **19:51:27 UTC**
and inspected **111 tables**. That ordinary copy does not run protected replay;
the separate 63-to-69 rehearsal provides that evidence.

The final production comparison preserves all original-column fingerprints across
the 107 pre-existing tables. Measurement choices, enabled choices, activity facts,
metric-view grants and metric-export grants remain zero. Production configuration
enables the feature with **America/Chicago** reporting; account collection still
requires explicit consent and current eligibility. No QA account, consent, grant,
content, export, or outbound message was created in production.

The canonical site passes **27 public/private-denial checks**, **four operational
health checks**, and **six actual signed-in Chrome observations**, without browser
errors. These verify the exact loaded renderer, versioned release and feature
guidance, privacy disclosure, narrow layouts, denied metric/API access, default-off
operational-account choice, scoped overview, and existing native report review.
The production Growth report's positive privileged path awaits actual operator
provisioning; positive reports, consent and exports are verified with isolated
fictional accounts. Physical phones are not claimed as tested.

## Runtime cost and continuation

Seven warm local samples return Growth at **12.647 ms median / 13.771 ms max**,
overview at **3.592 / 5.590 ms**, and saved choice at **1.700 / 1.730 ms**. Responses
are **10,088 / 558 / 297 bytes**, with **15–16 / 13–14 / 11–13 commands**. The
fixtures are small and fictional; these are not production capacity or SLA results.

Actual provider output has **359 lambda route entries and 12 distinct function
packages**, totaling **150,431,513 packaged bytes**. The previous release had
355 entries, 12 packages and 148,581,136 bytes. Functions use Node 24 and configured
2048 MB; ordinary functions remain in `iad1`, with middleware in eight regions.
Configured memory and packaged bytes are not billed memory or measured CPU use.

The dictionary/baseline and minimal measurement engineering scopes are complete.
The growth parent, report integration and shared acceptance stay open for the
specified feedback-owned ratings, prompt exposures and response funnel. Continue
feature 31 immediately, then shared admin acceptance and the next eligible
priorities. Reuse existing operator, account-manager, provider and physical-device
blockers. The continuous Extra High run remains active; final review stays last.
